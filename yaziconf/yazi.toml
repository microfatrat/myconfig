# ============================================================
# yazi.toml — Yazi 通用配置文件
# 覆盖此文件中的选项即可改变默认行为，不需要复制全部内容。
# 默认配置: https://github.com/sxyazi/yazi/tree/shipped/yazi-config/preset
# ============================================================

[mgr]
# 三栏布局比例 [parent, current, preview]，设为 0 隐藏对应面板
ratio          = [ 1, 4, 3 ]
# 排序方式: "none" | "mtime" | "btime" | "extension" | "alphabetical" | "natural" | "size" | "random"
sort_by        = "alphabetical"
# 是否区分大小写排序
sort_sensitive = false
# 是否反向排序
sort_reverse   = false
# 是否将目录排在文件前面
sort_dir_first = true
# 排序时音译特殊字符 (如 Â→A)，仅在 sort_by = "natural" 时有效
sort_translit  = false
# 排序回退方案
sort_fallback  = "alphabetical"
# 行尾信息: "none" | "size" | "btime" | "mtime" | "permissions" | "owner" 或自定义插件
linemode       = "none"
# 是否显示隐藏文件
show_hidden    = false
# 是否显示符号链接目标路径
show_symlink   = true
# 光标上下保留文件数，大于半屏高度时光标居中
scrolloff      = 5
# 启用的鼠标事件: "click" | "scroll" | "touch" | "move" | "drag"
mouse_events   = [ "click", "scroll", "drag" ]

[preview]
# 预览文本换行: "no" | "yes" | "word"
wrap            = "no"
# Tab 宽度
tab_size        = 2
# 预览区最大宽度 (像素)
max_width       = 600
# 预览区最大高度 (像素)
max_height      = 900
# 预览缓存目录 (空字符串使用默认)
cache_dir       = ""
# 图片加载延迟 (毫秒)，防止频繁切换时的性能问题
image_delay     = 30
# 图片缩放滤镜: "nearest" | "triangle" | "catmull-rom" | "lanczos3"
image_filter    = "triangle"
# 图片质量 (1-100，适用于 JPEG)
image_quality   = 75
# Ueberzug 缩放比例
ueberzug_scale  = 1
# Ueberzug 偏移量 [x, y, width, height]
ueberzug_offset = [ 0, 0, 0, 0 ]

[opener]
# 编辑器
edit = [
	{ run = "${EDITOR:-vi} %s", desc = "$EDITOR",      for = "unix", block = true },
	{ run = "zed %s",          desc = "zed",         for = "windows", orphan = true },
	{ run = "zed -w %s",       desc = "zed (block)", for = "windows", block = true },
]
# 媒体播放
play = [
	{ run = "xdg-open %s1",    desc = "Play", for = "linux", orphan = true },
	{ run = "open %s",         desc = "Play", for = "macos" },
	{ run = 'start "" %s1',    desc = "Play", for = "windows", orphan = true },
	{ run = "termux-open %s1", desc = "Play", for = "android" },
	{ run = "mediainfo %s1; echo 'Press enter to exit'; read _", block = true, desc = "Show media info", for = "unix" },
	{ run = "mediainfo %s1 & pause", block = true, desc = "Show media info", for = "windows" },
]
# 通用打开
open = [
	{ run = "xdg-open %s1",    desc = "Open", for = "linux" },
	{ run = "open %s",         desc = "Open", for = "macos" },
	{ run = 'start "" %s1',    desc = "Open", for = "windows", orphan = true },
	{ run = "termux-open %s1", desc = "Open", for = "android" },
]
# 在文件管理器中显示
reveal = [
	{ run = "xdg-open %d1",         desc = "Reveal", for = "linux" },
	{ run = "open -R %s1",          desc = "Reveal", for = "macos" },
	{ run = "explorer /select,%s1", desc = "Reveal", for = "windows", orphan = true },
	{ run = "termux-open %d1",      desc = "Reveal", for = "android" },
	{ run = "clear; exiftool %s1; echo 'Press enter to exit'; read _", desc = "Show EXIF", for = "unix", block = true },
]
# 解压
extract = [
	{ run = "ya pub extract --list %s", desc = "Extract here" },
]
# 下载
download = [
	{ run = "ya emit download --open %S", desc = "Download and open" },
	{ run = "ya emit download %S",        desc = "Download" },
]

# 文件打开规则 (按 MIME 类型 / URL 匹配对应的 opener)
[open]
rules = [
	# 目录
	{ url = "*/", use = [ "edit", "open", "reveal" ] },
	# 文本
	{ mime = "text/*", use = [ "edit", "reveal" ] },
	# 图片
	{ mime = "image/*", use = [ "open", "reveal" ] },
	# 媒体
	{ mime = "{audio,video}/*", use = [ "play", "reveal" ] },
	# 代码
	{ mime = "application/{json,ndjson,javascript,wine-extension-ini}", use = [ "edit", "reveal" ] },
	# 压缩包
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", use = [ "extract", "reveal" ] },
	# 空文件
	{ mime = "inode/empty", use = [ "edit", "reveal" ] },
	# 虚拟文件系统
	{ mime = "vfs/{absent,stale}", use = "download" },
	# 默认
	{ url = "*", use = [ "open", "reveal" ] },
]

[tasks]
# 文件操作工作线程数
file_workers     = 3
# 插件工作线程数
plugin_workers   = 5
# 网络获取工作线程数
fetch_workers    = 5
# 预加载工作线程数
preload_workers  = 2
# 进程管理工作线程数
process_workers  = 5
# 异常任务重试次数
bizarre_retry    = 3
# 图片内存分配 (字节, 512MB)
image_alloc      = 536870912
# 图片尺寸上限 [width, height]
image_bound      = [ 10000, 10000 ]
# 是否在大目录下抑制预加载
suppress_preload = false

[plugin]
# MIME 类型获取器
fetchers = [
	{ url = "*/",         run = "mime.dir",    prio = "high", group = "mime" },
	{ url = "local://*",  run = "mime.local",  prio = "high", group = "mime" },
	{ url = "remote://*", run = "mime.remote", prio = "high", group = "mime" },
]
# 文件信息探测器
spotters = [
	{ mime = "multi/*", run = "multi" },
	{ url = "*/", run = "folder" },
	# 代码
	{ mime = "text/*", run = "code" },
	{ mime = "application/{mbox,javascript,wine-extension-ini}", run = "code" },
	# 图片
	{ mime = "image/{avif,hei?,jxl}", run = "magick" },
	{ mime = "image/svg+xml", run = "svg" },
	{ mime = "image/*", run = "image" },
	# 视频
	{ mime = "video/*", run = "video" },
	# 虚拟文件系统
	{ mime = "vfs/*", run = "vfs" },
	# 错误
	{ mime = "null/*", run = "null" },
	# 默认
	{ url = "*", run = "file" },
]
# 预加载器 (生成缩略图等)
preloaders = [
	# 图片
	{ mime = "image/{avif,hei?,jxl}", run = "magick" },
	{ mime = "image/svg+xml", run = "svg" },
	{ mime = "image/*", run = "image" },
	# 视频
	{ mime = "video/*", run = "video" },
	# PDF
	{ mime = "application/pdf", run = "pdf" },
	# 字体
	{ mime = "font/*", run = "font" },
	{ mime = "application/ms-opentype", run = "font" },
]
# 预览器
previewers = [
	{ url = "*/", run = "folder" },
	# 代码
	{ mime = "text/*", run = "code" },
	{ mime = "application/{mbox,javascript,wine-extension-ini}", run = "code" },
	# JSON
	{ mime = "application/{json,ndjson}", run = "json" },
	# 图片
	{ mime = "image/{avif,hei?,jxl}", run = "magick" },
	{ mime = "image/svg+xml", run = "svg" },
	{ mime = "image/*", run = "image" },
	# 视频
	{ mime = "video/*", run = "video" },
	# PDF
	{ mime = "application/pdf", run = "pdf" },
	# 压缩包
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", run = "archive" },
	{ mime = "application/{debian*-package,redhat-package-manager,rpm,android.package-archive}", run = "archive" },
	{ url = "*.{AppImage,appimage}", run = "archive" },
	# 虚拟磁盘 / 磁盘镜像
	{ mime = "application/{iso9660-image,qemu-disk,ms-wim,apple-diskimage}", run = "archive" },
	{ mime = "application/virtualbox-{vhd,vhdx}", run = "archive" },
	{ url = "*.{img,fat,ext,ext2,ext3,ext4,squashfs,ntfs,hfs,hfsx}", run = "archive" },
	# 字体
	{ mime = "font/*", run = "font" },
	{ mime = "application/ms-opentype", run = "font" },
	# 空文件
	{ mime = "inode/empty", run = "empty" },
	# 虚拟文件系统
	{ mime = "vfs/*", run = "vfs" },
	# 错误
	{ mime = "null/*", run = "null" },
	# 默认
	{ url = "*", run = "file" },
]

[input]
# 光标是否闪烁
cursor_blink = false

# === 各输入框位置配置 ===
# origin: "top-left" | "top-center" | "top-right" | "bottom-left" | "bottom-center" | "bottom-right"
#         | "center" | "hovered"
# offset: [x, y, width, height]

# cd (跳转目录)
cd_title  = "Change directory:"
cd_origin = "top-center"
cd_offset = [ 0, 2, 50, 3 ]

# create (创建文件/目录)
create_title  = [ "Create:", "Create (dir):" ]
create_origin = "top-center"
create_offset = [ 0, 2, 50, 3 ]

# rename (重命名)
rename_title  = "Rename:"
rename_origin = "hovered"
rename_offset = [ 0, 1, 50, 3 ]

# filter (过滤)
filter_title  = "Filter:"
filter_origin = "top-center"
filter_offset = [ 0, 2, 50, 3 ]

# find (查找)
find_title  = [ "Find next:", "Find previous:" ]
find_origin = "top-center"
find_offset = [ 0, 2, 50, 3 ]

# search (搜索)
search_title  = "Search via {n}:"
search_origin = "top-center"
search_offset = [ 0, 2, 50, 3 ]

# shell (执行命令)
shell_title  = [ "Shell:", "Shell (block):" ]
shell_origin = "top-center"
shell_offset = [ 0, 2, 50, 3 ]

[confirm]
# 移到回收站确认
trash_title   = "Trash {n} selected file{s}?"
trash_origin  = "center"
trash_offset  = [ 0, 0, 70, 20 ]

# 永久删除确认
delete_title  = "Permanently delete {n} selected file{s}?"
delete_origin = "center"
delete_offset = [ 0, 0, 70, 20 ]

# 覆盖文件确认
overwrite_title  = "Overwrite file?"
overwrite_body   = "Will overwrite the following file:"
overwrite_origin = "center"
overwrite_offset = [ 0, 0, 50, 15 ]

# 退出确认
quit_title  = "Quit?"
quit_body   = "There are unfinished tasks, quit anyway?\n(Open task manager with default key 'w')"
quit_origin = "center"
quit_offset = [ 0, 0, 50, 15 ]

[pick]
# 选择打开方式
open_title  = "Open with:"
open_origin = "hovered"
open_offset = [ 0, 1, 50, 7 ]

[which]
# 候选面板排序
sort_by        = "none"
sort_sensitive = false
sort_reverse   = false
sort_translit  = false
